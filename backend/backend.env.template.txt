## Backend environment template
## Create `backend/.env` locally with these values.
## (This repo may block creating `.env` via automated tooling; create it manually in your editor.)

# Azure Blob Storage (required)
AZURE_STORAGE_ACCOUNT_NAME=your_account_name
AZURE_STORAGE_ACCOUNT_KEY=your_account_key

# Containers (incoming is required, verified is optional for retained flows only)
AZURE_STORAGE_CONTAINER_INCOMING=incoming-docs
AZURE_STORAGE_CONTAINER_VERIFIED=verified-docs

# API security (must match frontend)
API_TOKEN=veriquickx-secret-token-change-in-production

# AWS Rekognition face verification
# Prefer AWS profiles/roles for production. These env vars are convenient for local dev.
AWS_REGION=ap-south-1
AWS_REKOGNITION_REGION=ap-south-1
AWS_ACCESS_KEY_ID=replace-with-access-key
AWS_SECRET_ACCESS_KEY=replace-with-secret-key
# AWS_SESSION_TOKEN=replace-only-if-using-temporary-credentials

# Required when processing scanned PDF documents.  Set POPPLER_PATH to the
# directory containing pdfinfo.exe and pdftoppm.exe if it is not on PATH.
# POPPLER_PATH=C:\\Program Files\\poppler\\Library\\bin
# Set this only when tesseract.exe is not on PATH.
# TESSERACT_CMD=C:\\Program Files\\Tesseract-OCR\\tesseract.exe

# Optional SentinelTrail MySQL database package integration
# Keep disabled until MySQL 8.0 is installed and all files in mainapp/database/migrations
# have been applied in filename order.
SENTINEL_DB_ENABLED=false
SENTINEL_DB_STRICT=false
SENTINEL_DB_HOST=localhost
SENTINEL_DB_PORT=3306
SENTINEL_DB_NAME=sentineltrail
SENTINEL_DB_USER=talon_app
SENTINEL_DB_PASSWORD=replace-with-mysql-password
SENTINEL_DB_SSL_DISABLED=true
SENTINEL_ORGANIZATION_CODE=TALON-DEMO
SENTINEL_ORGANIZATION_NAME=Talon Demo Authority
SENTINEL_CHECKPOINT_CODE=DEMO-GATE-1
SENTINEL_CHECKPOINT_NAME=Demo Verification Gate
SENTINEL_CHECKPOINT_COUNTRY=IN
SENTINEL_ISSUING_STATE_CODE=IND
SENTINEL_CHECKPOINT_TIMEZONE=Asia/Kolkata
SENTINEL_OFFICER_SUBJECT=talon-demo-officer
SENTINEL_OFFICER_NAME=Talon Demo Officer
SENTINEL_DEVICE_PUBLIC_ID=talon-mainapp-workstation
SENTINEL_DOCUMENT_RETENTION_DAYS=7
SENTINEL_BIOMETRIC_RETENTION_HOURS=12

# Optional Next.js dashboard event publishing. Keep disabled for standalone mainapp mode.
DASHBOARD_SYNC_ENABLED=false
DASHBOARD_URL=http://localhost:3000
SCANNER_TOKEN=replace-with-the-dashboard-SCANNER_TOKEN
SCANNER_SIGNING_SECRET=replace-with-the-dashboard-SCANNER_SIGNING_SECRET

